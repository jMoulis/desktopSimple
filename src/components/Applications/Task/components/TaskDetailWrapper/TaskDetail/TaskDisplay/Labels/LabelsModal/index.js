import React from 'react';
import PropTypes from 'prop-types';
import './index.css';

const LabelsModal = ({ children }) => {
  return <div className="labels-modal">{children}</div>;
};

LabelsModal.propTypes = {};

export default LabelsModal;
