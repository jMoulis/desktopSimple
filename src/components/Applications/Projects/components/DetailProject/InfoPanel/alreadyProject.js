import React from 'react';

const AlreadyProject = () => (
  <div className="teams-container-buttons">
    <h1>Your team has already a project</h1>
    <p>As a team you only can have an active project at a time</p>
  </div>
);

export default AlreadyProject;
